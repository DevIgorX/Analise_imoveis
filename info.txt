 id_corretor = db.Column(db.Integer, db.ForeignKey('corretores.id'), nullable=False)
    corretor = db.relationship('Corretor', backref='imoveis')

    "id_corretor: número que referencia o corretor (coluna real no banco).
    corretor: atalho do SQLAlchemy para acessar diretamente o objeto Corretor relacionado.
    backref='imoveis': dá acesso inverso, permitindo que o corretor enxergue todos os imóveis associados a ele."